name: Build & Test

on:
  push:
    branches:
      - feature/*
      - bugfix/*
      - hotfix/*
    paths:
      - 'packages/web-app/**'

jobs:
  build_and_test:
    uses: './reusable-build-and-test.yml'
    with:
      path: 'web-app'
